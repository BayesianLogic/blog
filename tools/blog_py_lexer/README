blog-lexer
==========

A pygments lexer for BLOG


To install:
    pip install .


This package requires pygments package. You may install by 
    pip install Pygments


If you do not have pip, you may install manually 
    sudo python setup.py install 