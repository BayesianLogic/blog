type Date;
distinct Date Day[365];

type Person;

random Date Birthday(Person p) 
  ~ UniformChoice({Date d});

#Person ~ Poisson(13);

obs #Person = 23;
query exists Person x exists Person y  
  ((x != y) & (Birthday(x) == Birthday(y)));
