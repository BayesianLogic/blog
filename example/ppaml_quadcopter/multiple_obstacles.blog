type Obstacle;

#Obstacle ~ Poisson(5);

random Real obstX(Obstacle o) ~ UniformReal(-7, 7);
random Real obstY(Obstacle o) ~ UniformReal(-7, 7);
fixed Real obstR = 1;

/* query fry({obstX(o), obstY(o), obstR for Obstacle o}); */

/* random Real foo = fry({obstX(o), obstY(o), obstR for Obstacle o}); */
/* query foo; */

random Set coords = {obstX(o), obstY(o), obstR for Obstacle o};
query coords;
